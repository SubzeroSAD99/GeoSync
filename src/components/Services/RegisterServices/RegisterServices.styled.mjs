import styled from "styled-components";

const Title = styled.h2`
  border-bottom: 2px solid var(--main-color);
  width: max-content;
  margin: 0px auto;
  overflow: hidden;
  flex-shrink: 0;
`;

export { Title };
