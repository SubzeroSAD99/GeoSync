import styled from "styled-components";

const Title = styled.h2({
  borderBottom: "2px solid var(--main-color)",
  width: "max-content",
  margin: "0px auto",
  overflow: "hidden",
  flexShrink: "0",
});

export { Title };
